map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;
    client_max_body_size 2g;

    gzip on;
    gzip_types text/plain text/css application/javascript application/json image/svg+xml;

    location = /api/version {
        alias /usr/share/nginx/html/version.json;
        default_type application/json;
        add_header Cache-Control "no-cache";
    }

    location = /api/version/web {
        alias /usr/share/nginx/html/version-web.json;
        default_type application/json;
        add_header Cache-Control "no-cache";
    }

    location = /api/version/server {
        proxy_pass http://typetype-server:8080/version;
        add_header Cache-Control "no-cache";
    }

    location = /api/version/token {
        proxy_pass http://typetype-token:8081/version;
        add_header Cache-Control "no-cache";
    }

    location = /api/version/downloader {
        proxy_pass http://typetype-downloader:18093/version;
        add_header Cache-Control "no-cache";
    }

    location ^~ /api/ {
        client_max_body_size 2g;
        proxy_pass http://typetype-server:8080/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 300;
        proxy_send_timeout 300;
        proxy_buffering off;
    }

    location ^~ /sabr/ {
        proxy_pass http://typetype-server:8080/sabr/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 300;
        proxy_send_timeout 300;
        proxy_buffering off;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
