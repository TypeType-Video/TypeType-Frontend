{
  "name": "@typetype/frontend",
  "version": "1.8.1",
  "devDependencies": {
    "@biomejs/biome": "^2.5.12",
    "knip": "^6.35.0",
    "sherif": "^1.13.0"
  },
  "overrides": {
    "@babel/core": "^7.29.7",
    "picomatch": "^2.3.2",
    "postcss": "8.5.26"
  },
  "packageManager": "bun@1.4.2",
  "private": true,
  "scripts": {
    "localize": "bun run --cwd apps/web localize",
    "localization:report": "node scripts/localization-report.mjs",
    "dev": "bun run --cwd apps/web dev",
    "build": "bun run --cwd apps/web build",
    "check": "bun run localize && node scripts/check-localization.mjs && biome check .",
    "format": "biome format --write .",
    "knip": "knip",
    "sherif": "sherif",
    "test": "bun run localize && bun run --cwd apps/web test",
    "test:coverage": "bun run localize && bun run --cwd apps/web test:coverage"
  },
  "workspaces": [
    "apps/*"
  ]
}
